# Archived: ATP clean output (schema v1)

Superseded by the unified ATP/WTA schema described in
[docs/tennis-data-uk-pipeline-plan.md](../../../../docs/tennis-data-uk-pipeline-plan.md).

Kept for reference/diffing only - do not build on this. Known issues with this
version:

- Generated by a version of `handler/uk/cleaner/atp.py` that had a broken
  relative import (`from ....cleaner.uk import atp_cols`), so it's unclear
  which code revision actually produced these files.
- `is_outdoor` here is the string category `"outdoor"`/`"indoor"`; current
  code produces a real boolean.
- No WTA equivalent ever existed.
